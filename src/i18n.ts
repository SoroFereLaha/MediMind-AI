
// This file is no longer used as internationalization has been removed
// to simplify the project to French-only.
// It can be safely deleted.
console.log('################# [i18n.ts] THIS FILE IS NO LONGER USED AND CAN BE DELETED #################');

export const locales = ['fr'];
export const defaultLocale = 'fr';

// The getRequestConfig function is no longer needed.
// export default getRequestConfig(async ({locale}) => { ... });

export {}; // To make it a valid module if not deleted immediately
